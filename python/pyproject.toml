[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vlangx"
version = "1.1.0"
description = "Python prototype tools for Vector Language"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["vlangx*"]
