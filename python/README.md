# Python prototype

This package contains a lightweight parser/compiler/runtime prototype for the
Vector Language.

It is intended for:

- syntax experiments
- sample compilation tests
- quick interactive validation

Run a sample:

```bash
python -m vlangx.prototype ../examples/treasury.vx
```
